---
title: API reference (superseded)
description: This page has been split into several focused pages under /core/api/ — see core/api/index.md and its siblings.
---

**This file is stale and needs to be deleted.** Its content was split
into `core/api/index.md`, `core/api/lifecycle.md`,
`core/api/chart-kinds.md`, `core/api/plugins.md`, and
`core/api/test-utils.md`, at the user's explicit request, for
readability. Left with this placeholder content rather than the real
content it used to have (this Filesystem connector has no delete
capability) — **please delete `docs/site/src/content/docs/core/api.md`
yourself.** Until it's deleted, it and `core/api/index.md` both resolve
to the same slug (`core/api`), which Astro's content collections may
not handle gracefully (undefined behavior — possibly a build error,
possibly one silently winning over the other).
