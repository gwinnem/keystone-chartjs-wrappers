---
title: Annotation plugin
description: chartjs-plugin-annotation, supplied via the annotation prop, drawing lines, boxes, points, labels, polygons, and ellipses on the chart area.
---
import AnnotationPluginSource from '../../../../components/examples/annotation-plugin.vue?raw';
import ExampleTryIt from '../../../../components/harness/ExampleTryIt.astro';

The `annotation` prop wires in the real `chartjs-plugin-annotation`
package, dynamically imported and registered the first time any
`<Chart annotation="...">` mounts — one of the two remaining
still-dependency-based plugins in this library (alongside
`dataLabels`), unlike `zoom`/`gradient`/`hierarchical`/`imageLabel`,
which are all local ports.

:::caution[Not yet live-embedded on this docs site]
This example isn't hydrated on this page yet — this docs site resolves
`keystone-chartjs-core` from this monorepo's own workspace source, not a
published package, and the dynamic import that lazily registers
still-dependency-based plugins like `annotation` doesn't currently
resolve correctly across that boundary in Vite's dev server. The code
below is real and correct — confirmed working end-to-end in this
package's own Playwright e2e suite
(`packages/vue/tests/e2e/annotation-plugin.spec.ts`) — this is a
docs-site build-pipeline gap, not a bug in `keystone-chartjs-vue`
itself. Expected to resolve once this package is built/published for
real (Phase 7).
:::

<ExampleTryIt code={AnnotationPluginSource} filename="annotation-plugin.vue" live={false} />

:::tip[No plain-boolean form, unlike zoom/dataLabels]
`annotation` has no boolean opt-in form — `AnnotationPluginOptions.annotations`
is required, since there's no sensible empty default (the same reasoning
as `imageLabel`'s own `imagesList`). Its real config merges into
`options.plugins.annotation.annotations`. See
[Plugins](/vue/api/plugins/) for the full prop reference.
:::
