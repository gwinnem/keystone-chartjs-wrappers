// keystone-chartjs-angular — public entry point (barrel export).
// A missing/incomplete barrel export here bit keystone-dashboard-layout's
// Angular package before — keep every public symbol re-exported here.
// Scaffolding only; see docs/IMPLEMENTATION_PLAN.md Phase 4.
export { KeystoneChartComponent } from './keystone-chart.component.js';
