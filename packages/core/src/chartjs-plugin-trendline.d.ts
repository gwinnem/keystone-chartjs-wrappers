// Obsolete — `chartjs-plugin-trendline` is no longer a dependency of
// this project at all (ported directly into
// `src/plugins/trendline/`, at your explicit request, so this package
// depends on nothing but `chart.js` itself). The ambient declaration
// this file once held (working around the real package shipping no
// TypeScript declarations) is no longer needed for the same reason.
export {};
